# Captain's Log App Store Completion Audit

Audit date: May 19, 2026

This audit restates the current objective as concrete completion criteria and maps each criterion to the evidence that exists today. It is intentionally stricter than a progress summary: if a requirement needs App Store Connect, signing, legal review, or human approval, it remains open.

## Completion Criteria

Captain's Log is ready for the first App Store Connect/TestFlight pass only when:

1. The current source tree, linked Kit941 package, metadata, privacy manifest, screenshots, and release helper scripts are locally verified.
2. A current App Store Connect IPA exists from clean source and passes local release checks.
3. App Store Connect API credentials and distribution signing are available outside the repo.
4. The App Store Connect app record exists and matches the bundle ID, expected SKU, and expected name.
5. Product metadata, privacy answers, screenshots, and review notes are entered and accepted by App Store Connect.
6. The build validates, uploads, and finishes TestFlight processing.
7. The screenshot/design set receives human approval.
8. Legal/privacy review and final real-account tap-through are complete.

## Prompt-To-Artifact Checklist

| Requirement | Artifact or command | Current evidence | Status |
| --- | --- | --- | --- |
| Prepare for App Store Connect | `README.md`, `Docs/AppStoreConnectRunbook.md`, `Docs/AppStoreConnectSubmission.md`, `Docs/AppStoreReadiness.md` | Root handoff, runbook, evidence packet, metadata, privacy answers, screenshot packet, export helper, upload helper, and readiness script exist. Readiness verifies CaptainsLog and linked Kit941 source cleanliness and reports upstream drift before submission work. | Partially complete |
| Clean up UI | `/tmp/captainslog-appstore-review/contact-sheet.png`, `/tmp/captainslog-key-state-audit`, `Scripts/open_app_store_screenshot_review.sh`, `Docs/AppStoreDesignReview.md`, `Scripts/audit_app_store_screenshot_text.sh /tmp/captainslog-key-state-packaged` | Screenshot packet shows dashboard, Work Map, journal, repositories, AI providers, and Privacy & Data on iPhone and iPad. The dashboard uses the compact selected-period menu instead of a second full-width segmented control, repository access now splits summary/search from the repo list on iPad, the demo journal copy no longer exposes `fixture`/`UI Fixture` wording, and the repeatable screenshot text audit found no debug, simulator, sync-progress, error, personal-account, or token-like text in the regenerated packaged screenshots. `Scripts/open_app_store_screenshot_review.sh` validates and opens the review page, contact sheet, and checklist for the remaining human approval. A Yurei browser-rendered spot-check of the review page and contact sheet found no obvious private data, live keys, simulator chrome, debug labels, active sync bars, or clipped controls. | Locally acceptable, human approval open |
| Make design review happy | `.impeccable.md`, `Docs/AppStoreDesignReview.md`, `.codex/plans/cave-plan-app-store-gate.md`, `/tmp/codex-cave-session/validation.json` | Design context says quiet, precise, journal-like, Apple-native, with Work Map as the identity surface. `$cave` was requested; tool discovery did not expose it, but the local Cave skill was found and run against the App Store screenshot/release evidence. Cave emitted an ignored private plan and the emission gate passed with 4 cuts, 2 disagreements, 14 citations, no banned-token hits, no missing sections, and no missing session files. Cave's verdict matches the design review: no new features before TestFlight, keep the current screenshot set unless human preview rejects it, and close release gates first. Design review score is 33/40, the latest spot-check says the dashboard control weight improved and the repository iPad split reduced an underused tablet composition. | Locally acceptable; human approval open |
| Metadata and manual App Store fields | `Docs/AppStoreMetadata.md` | Name, subtitle, description, keywords, URLs, review notes, screenshot order, price/distribution/release recommendations, platform-availability recommendations, and manual App Store Connect choices are documented. | Paste-ready, App Store entry open |
| Privacy answers | `Docs/AppStorePrivacyAnswers.md`, `Docs/PrivacyPolicyDraft.md`, `Docs/SupportPageDraft.md` | Privacy questionnaire draft exists; published support/privacy URLs passed preflight content checks. Legal review remains recommended. | Locally ready, legal review open |
| Required reason API and privacy manifest | `Scripts/privacy_required_reason_audit.sh`, `CaptainsLog/Resources/PrivacyInfo.xcprivacy` | Latest readiness run passed required-reason audit and preflight confirmed UserDefaults reason `CA92.1`. | Complete locally |
| App icons, screenshots, and release plist surface | `Scripts/app_store_preflight.sh /tmp/captainslog-key-state-audit`, `Scripts/app_store_readiness_status.sh` | Preflight passed icon dimensions, all app icon alpha-channel checks, source iPhone/iPad screenshot dimensions, export-compliance flag, privacy manifest, and background-processing task identifier consistency. Readiness also verifies the packaged upload folders contain the exact six expected screenshot filenames per family with 6.9-inch iPhone and accepted 13-inch iPad portrait dimensions. | Complete locally |
| iPad availability | `project.yml`, `xcodebuild -project CaptainsLog.xcodeproj -scheme CaptainsLog-iOS -showBuildSettings`, packaged iPad screenshots | `project.yml` sets `TARGETED_DEVICE_FAMILY: "1,2"`; current build settings report `TARGETED_DEVICE_FAMILY = 1,2`; readiness reports `[ok] iPhone and iPad enabled through TARGETED_DEVICE_FAMILY=1,2`; the screenshot package contains six accepted 13-inch iPad PNGs. | Locally ready; signed IPA/upload still open |
| Apple Vision Pro availability | `Scripts/smoke_vision_compatible_launch.sh`, `xcodebuild -showBuildSettings`, visionOS simulator launch evidence, `Docs/AppStoreReadiness.md` | Current build settings report `SUPPORTS_XR_DESIGNED_FOR_IPHONE_IPAD = YES`; readiness reports `[ok] Apple Vision Pro compatible iPhone/iPad availability is supported by build settings`. The latest May 18, 2026 strict-keychain smoke run rebuilt the no-sign Release compatible app, installed and launched process `35768` on Vision simulator `37661BC9-7032-40F6-842F-1D2A647C2899`, captured `/tmp/captainslog-vision-smoke/vision-compatible-launch.png` at `3840x2160`, wrote 10 OCR lines, found Captain's Log, the GitHub sign-in action, and Use Demo Data by OCR, and did not find `Keychain returned status -34018`. | Compatible path locally verified; signed TestFlight/auth UX still open |
| Mac availability | `Scripts/smoke_macos_launch.sh`, `Scripts/capture_macos_app_store_screenshots.sh`, `Scripts/export_macos_app_store_pkg.sh`, `Scripts/check_app_store_connect_record.py --bundle-id com.blakecrosley.captainslog.mac --entitlements CaptainsLog/App/CaptainsLog.entitlements --skip-app-record --require capabilities`, `Scripts/ensure_platform_bundle_ids.py`, `xcodebuild -list -project CaptainsLog.xcodeproj`, `xcodebuild -showBuildSettings`, `project.yml`, `Docs/AppStoreReadiness.md` | `xcodebuild -list` shows `CaptainsLog-macOS`; `project.yml` defines the native macOS target with bundle ID `com.blakecrosley.captainslog.mac`, automatic signing for team `M4WTLM6RAQ`, and hardened runtime. The latest local Mac verification includes readiness evidence that Release build settings report the expected bundle ID, `CODE_SIGN_STYLE = Automatic`, `DEVELOPMENT_TEAM = M4WTLM6RAQ`, and `ENABLE_HARDENED_RUNTIME = YES`; the no-sign Release smoke artifacts record bundle ID `com.blakecrosley.captainslog.mac`, version `1.0.0 (1)`, category `public.app-category.developer-tools`, launched process `58890`, clean quit, and `TeamIdentifier=not set` because signing is intentionally disabled for the smoke. The Mac screenshot script builds the Debug macOS app in screenshot mode, captures six app-window-only PNGs at `2880x1800`, writes a manifest, and passes the screenshot OCR text audit. The Mac export script stages archive/package output and writes `MacExportManifest.txt` after validation. A May 19, 2026 REST bundle/capability check reports the native Mac Developer Portal bundle ID `com.blakecrosley.captainslog.mac` is missing or not visible to this API key, so required capabilities and profiles cannot be complete yet. `Scripts/ensure_platform_bundle_ids.py` dry-runs the missing Mac bundle ID creation plus `ICLOUD` enablement; `--apply` requires `--confirm-team M4WTLM6RAQ`, rechecks exact bundle/capability visibility after mutation, and has not been run. A May 18, 2026 precheck-bypassed Mac export reached `xcodebuild`, archived successfully with Apple Development signing, then failed export with `No Accounts`, no `Mac Installer Distribution` certificate, and no profiles for `com.blakecrosley.captainslog.mac`; strict export still fails earlier without Mac App Store application/installer signing or App Store Connect API auth. Current readiness still warns Mac requires a native Mac bundle ID, signed Mac App Store export, screenshot marketing acceptance, TestFlight, and human QA before Mac App Store availability. | Target exists; not Mac App Store ready |
| Apple Watch and Apple TV availability | `xcodebuild -list -project CaptainsLog.xcodeproj`, `project.yml`, `Scripts/app_store_readiness_status.sh`, `Scripts/check_app_store_connect_record.py --bundle-id com.blakecrosley.captainslog.watchkitapp --entitlements CaptainsLogCompanion/CaptainsLogCompanion.entitlements --skip-app-record --require capabilities`, `Scripts/check_app_store_connect_record.py --bundle-id com.blakecrosley.captainslog.tv --entitlements CaptainsLogCompanion/CaptainsLogCompanion.entitlements --skip-app-record --require capabilities`, `Scripts/ensure_platform_bundle_ids.py`, `Scripts/smoke_watchos_launch.sh`, `Scripts/smoke_tvos_launch.sh`, `Scripts/capture_watchos_app_store_screenshots.sh`, `Scripts/capture_tvos_app_store_screenshots.sh`, generic no-sign `xcodebuild archive` commands, `xcodebuild -list -project ../Return/Return.xcodeproj`, `xcodebuild -list -project "../Banana List/Banana List.xcodeproj"` | `xcodebuild -list` now shows `CaptainsLog-watchOS` and `CaptainsLog-tvOS` targets/schemes for Captain's Log. `project.yml` defines both companion targets with Captain's Log bundle IDs, automatic signing team `M4WTLM6RAQ`, generated Info.plists, privacy manifests, shared source, and iCloud key-value entitlements. Watch selects the `AppIcon` asset catalog with a `1024x1024` marketing icon. TV selects the `App Icon & Top Shelf Image` brand asset catalog with checked app-icon and top-shelf image dimensions. `CaptainsLogShared` defines a privacy-safe aggregate snapshot; the main app writes it to iCloud key-value storage and pushes it to Watch through WatchConnectivity, Watch can request the latest phone snapshot, and TV reads the iCloud snapshot without GitHub credentials on the shared screen. Unsigned Release simulator smokes build, install, launch, screenshot, and OCR both companion targets. The watchOS and tvOS screenshot capture wrappers now stage App Store-sized screenshots, manifests, and OCR text-audit logs. Current-head generic no-sign Release archives also succeeded for watchOS and tvOS, proving device-platform archive packaging while leaving signing/export unproved. May 19, 2026 REST bundle/capability checks report both companion Developer Portal bundle IDs, `com.blakecrosley.captainslog.watchkitapp` and `com.blakecrosley.captainslog.tv`, are missing or not visible to this API key, so profile generation and required `ICLOUD` capability proof remain blocked for those platforms. `Scripts/ensure_platform_bundle_ids.py` dry-runs creation of the missing Watch and TV bundle IDs plus `ICLOUD` enablement; `--apply` requires `--confirm-team M4WTLM6RAQ`, rechecks exact bundle/capability visibility after mutation, and has not been run. Return provides a working local precedent with `ReturnWatch Watch App` and `ReturnTV` targets/schemes, and Get Bananas provides a watch app precedent through the local `Banana List` Xcode project, but those are still separate apps. Captain's Log Watch and TV remain not App Store-ready because they still need Developer Portal bundle IDs, required capabilities, signed archive/export, TestFlight, provisioning validation, human screenshot acceptance, and platform QA. | Assets/screenshots/archive local; Developer Portal bundle IDs missing; not App Store-ready |
| Preserve screenshot package/review artifacts on failed regeneration | `Scripts/package_app_store_screenshots.sh`, `Scripts/make_app_store_screenshot_contact_sheet.sh` | Both scripts now generate into staged temporary folders and replace the current package/review output only after generation succeeds. Smoke tests produced the 12-PNG package plus review HTML/contact sheet, and simulated missing-screenshot failures preserved the existing output folder. | Complete locally |
| Current release IPA | `CAPTAINS_LOG_REQUIRE_CLEAN_EXPORT=1 Scripts/export_app_store_ipa.sh /tmp/captainslog-current-appstore-export` | The export script checks Xcode 26+, iOS 26+ SDK, and distribution signing for team `M4WTLM6RAQ` before archiving. It can also pass `APP_STORE_CONNECT_API_KEY`, `APP_STORE_CONNECT_API_ISSUER`, and a matching `.p8` path to `xcodebuild` for App Store Connect-authenticated provisioning updates when those env vars are set; the path is inferred from `~/.appstoreconnect/private_keys/AuthKey_<KEY_ID>.p8` when omitted. The clean export guard requires a clean CaptainsLog tree and clean linked Kit941 package inputs (`Package.swift`, `Package.resolved`, and `Sources/Kit941`); dirty Kit941 files outside linked package source are recorded in the manifest but no longer block export. Because this machine has multiple staged `.p8` candidates, readiness, export, and upload still fail early if the selected key path does not match `AuthKey_<APP_STORE_CONNECT_API_KEY>.p8`, unless the mismatch override is explicitly set after manual verification. The May 19, 2026 readiness run still reports the IPA and export manifest missing. A May 19, 2026 export run using an existing 941-style App Store Connect API-key setup reached `xcodebuild`, archived successfully with Apple Development signing, then failed `xcodebuild -exportArchive` with `Cloud signing permission error`, `No signing certificate "iOS Distribution" found`, and the App Store provisioning profile missing the app's iCloud key-value store entitlement. After that stale local Store profile was moved aside, a clean export retry still archived successfully but failed export with `Cloud signing permission error` and `No profiles for 'com.blakecrosley.captainslog' were found`. `/tmp/captainslog-current-appstore-export` remained empty. The previous IPA is stale after app icon resources changed; readiness also fails if an archive-only output exists without the matching export manifest so stale development-signed archives are not mistaken for release output. | Blocked |
| Preserve existing IPA on failed export | `Scripts/export_app_store_ipa.sh` | Temp preservation test kept an existing dummy IPA and manifest after the expected signing failure. The script now stages archive, IPA, and export manifest together and only replaces current export output after validation and manifest creation succeed. | Complete locally |
| Upload helper local checks | `Scripts/upload_app_store_ipa.sh local-check <ipa>` | Helper validates bundle ID, privacy manifest, encryption flag, `get-task-allow=false`, clean export manifest, and debug/test fixture strings, including screenshot hooks and UI-testing launch hooks. It cannot pass until a current IPA exists. | Script ready, IPA blocked |
| App Store Connect app record check | `Scripts/check_app_store_connect_record.py`, `Scripts/upload_app_store_ipa.sh app-record`, `APP_STORE_CONNECT_PROVIDER_PUBLIC_ID` | The REST API status path uses the selected App Store Connect API-key inputs without requiring an altool provider public ID. It exact-matches Developer Portal bundle IDs, checks the app relationship for the exact bundle resource when available, records `appRecordLookupMethod` in JSON output, and requires the returned app record to match expected SKU `captainslog-ios` and app name `Captain's Log`. Current REST evidence returns the Developer Portal bundle ID `com.blakecrosley.captainslog` as existing for team `M4WTLM6RAQ`, confirms the required `ICLOUD` bundle capability is enabled, reports `appRecordLookupMethod: bundle-id-relationship-empty`, and reports zero visible matches for the expected SKU and name. Existing App Store records for Return, Get Bananas, or other 941 apps are account precedent only; Apple documents the `apps` API as a management surface for existing apps, so Captain's Log still needs its own web-created app record followed by read-only REST verification. `Scripts/upload_app_store_ipa.sh app-record` now uses the same REST check by default; `app-record-altool` remains available for the older `altool --list-apps` path when `APP_STORE_CONNECT_PROVIDER_PUBLIC_ID` is available. | Blocked: app record missing or not visible |
| App Store Connect API key custody | `Scripts/upload_app_store_ipa.sh credential-guard-self-test`, `Scripts/app_store_readiness_status.sh`, `.gitignore` | Credential guard self-test passed. Readiness confirms no `.p8` private-key material is in the repo. The current machine has 4 candidate `AuthKey_*.p8` private-key files staged outside the repo under `~/.appstoreconnect/private_keys`, and the repo-local gitignored `AppStoreConnectEnv.local.sh` now selects the current key/issuer/path for default readiness, export, upload, signing-status, and App Store Connect REST checks. Current signing status validates the key ID shape, issuer UUID shape, private-key header, owner-staged path outside the repo and outside git working trees, and filename match. Captain's Log accepts the same Fastlane aliases used by Return and Get Bananas (`ASC_KEY_ID`, `ASC_ISSUER_ID`, `ASC_KEY_PATH`) and now loads gitignored `AppStoreConnectEnv.local.sh` files from the repo root or `Docs/`, plus an explicit `CAPTAINS_LOG_APP_STORE_CONNECT_ENV_FILE` override, across the release scripts and App Store Connect REST checker. Existing 941 apps confirm the same credential convention, but Captain's Log still needs cloud-managed distribution certificate access or local distribution identities before export can succeed. `.gitignore` ignores local env files, `private_keys/`, `.p8`, `.mobileprovision`, `.provisionprofile`, `.cer`, and `.p12` files so common App Store signing artifacts are not staged accidentally. Reusing a key from another app must still use a private copy outside any git working tree; the credential guard rejects keys inside app repos or Fastlane folders. | Credential custody complete locally; cloud certificate access or local distribution identities open |
| Source token-literal hygiene | `Scripts/app_store_readiness_status.sh` | Readiness scans the app, tests, docs, and scripts for OpenAI/Anthropic/GitHub-token-shaped literals and currently reports `no token-shaped source literals found`. Screenshot and test fixtures construct fake key values at runtime instead of storing token-shaped source strings. | Complete locally |
| Distribution signing | `Scripts/app_store_signing_status.sh`, `Scripts/check_remote_signing_assets.py`, `security find-identity -v -p codesigning`, `Scripts/export_app_store_ipa.sh`, `Scripts/export_macos_app_store_pkg.sh`, and readiness script | The latest signing investigation passed the Xcode 26.5/toolchain check and found Apple Development plus Developer ID Application identities, but no Apple Distribution/iOS Distribution identity, Mac App Store application identity, or Mac App Store installer identity for team `M4WTLM6RAQ`. `security find-certificate -a -c "Apple Distribution"` found 0 Apple Distribution certificates. When existing 941-style API-key values are supplied, signing status validates the key/issuer/path shape and private-key custody, then warns that export still depends on cloud-managed distribution certificate access because local distribution identities are missing. The updated signing-status script now reports bundle-specific provisioning matches, flags missing required profile entitlements, and warns when the only matching iOS profile is development/ad-hoc. Current local state has one development iOS profile matching `com.blakecrosley.captainslog`, no local App Store profile for that bundle ID, and no local profiles matching the native Mac, Watch, or TV bundle IDs. The read-only remote signing checker now reports 0 visible usable iOS distribution certificates, 0 visible usable Mac App Store application certificates, 0 visible usable Mac installer certificates, one visible `IOS_APP_STORE` profile for `com.blakecrosley.captainslog` in `INVALID` state, and missing native Mac, Watch, and TV bundle IDs. The checker now applies target-specific certificate requirements so a future Mac-only check still requires Mac App Store application and installer certificate visibility. Apple's current Profile API does not document a dedicated watchOS App Store profile type, so the checker does not treat Watch profile state as complete until signed export/TestFlight proves it. The Mac App Store package path remains unverified under these credentials. | Blocked |
| Validate/upload/status | `Scripts/upload_app_store_ipa.sh validate`, `upload`, `status` | Cannot run until a current signed IPA exists. App Store Connect credentials are now selected through the gitignored local env, but upload/status still need a created app record plus a processed build to produce meaningful results. | Blocked |
| Final App Store Connect entry | App Store Connect web UI, `Docs/AppStoreConnectRunbook.md` | Manual fields, regional prompts, Apple Vision Pro availability, Apple Silicon Mac availability, EU DSA trader status, tax category if shown, demo/review contact, screenshot upload, processed build selection, and the final before-Add-for-Review warning check are not verifiable locally. | Human/App Store gate |
| Linked package source custody | `Scripts/app_store_readiness_status.sh`, `git -C ../941Kit status --short --branch`, `git -C ../941Kit diff --name-only origin/main..HEAD`, generic CaptainsLog iOS Release build | Kit941 is clean and synced with `origin/main` at `47a67ba Align experimental examples with app slices`; `git -C ../941Kit diff --name-only origin/main..HEAD` returned no paths. The earlier linked package commits, including `6f66986 Route default accents through Kit941 theme`, are now upstream. A current CaptainsLog generic iOS Release build with signing disabled succeeded at `/tmp/captainslog-current-ios-release-kit941-47a67ba`, proving the current synced Kit941 head still compiles into CaptainsLog. Final export still records the exact package commit and dirty state in the export manifest. | Complete locally |
| Current-head simulator tests | `xcodebuild test -project CaptainsLog.xcodeproj -scheme CaptainsLog-iOS -destination 'id=277C8808-F02C-43A4-8B4A-11BA187F0788' -derivedDataPath /tmp/captainslog-demo-path-unit-tests -only-testing:CaptainsLogTests`, `xcodebuild test -project CaptainsLog.xcodeproj -scheme CaptainsLog-iOS -destination 'id=277C8808-F02C-43A4-8B4A-11BA187F0788' -derivedDataPath /tmp/captainslog-demo-path-ui-tests -only-testing:CaptainsLogUITests` | Current source passed 69 unit tests and 3 UI tests with 0 failures on the iOS 26.5 iPhone 17 simulator. The UI tests cover first-run primary actions, the production Use Demo Data tap-through into dashboard and journal detail, fixture dashboard, Settings, Privacy & Data, and selected-day journal detail. | Complete locally |
| Current-head unsigned device archive | `xcodebuild archive -project CaptainsLog.xcodeproj -scheme CaptainsLog-iOS -configuration Release -destination 'generic/platform=iOS' -archivePath /tmp/captainslog-current-nosign-archive-refresh.xcarchive -derivedDataPath /tmp/captainslog-current-nosign-archive-refresh-derived CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=''` | The refreshed May 18, 2026 archive succeeded from current source with the linked local `Kit941` package. The archived app metadata reported bundle ID `com.blakecrosley.captainslog`, version `1.0.0 (1)`, `DTSDKName=iphoneos26.5`, `MinimumOSVersion=26.0`, `ITSAppUsesNonExemptEncryption=false`, `PrivacyInfo.xcprivacy` present, and `Assets.car` present. This proves current source/package/resources archive for device, but it is unsigned and does not replace the signed App Store IPA/export-manifest gate. | Complete as compile/package proof; signed IPA still blocked |
| Release simulator string scan | `xcodebuild build -project CaptainsLog.xcodeproj -scheme CaptainsLog-iOS -configuration Release -destination 'id=277C8808-F02C-43A4-8B4A-11BA187F0788' -derivedDataPath /tmp/captainslog-demo-path-release`, `strings <Release app executable>` | Release simulator build succeeded on the iOS 26.5 iPhone 17 simulator. The built executable did not contain the debug/test fixture strings that the future IPA `local-check` rejects, including screenshot hooks, debug OpenAI key seeding hooks, UI-testing hooks, and the screenshot demo key. Bundle metadata also showed the expected bundle ID, version/build, export-compliance flag, and privacy manifest. | Complete locally; signed IPA local-check still required |
| Final product QA | Real account on device, `xcodebuild test -project CaptainsLog.xcodeproj -scheme CaptainsLog-iOS -destination 'id=00008150-00166D690EF0401C' -derivedDataPath /tmp/captainslog-current-device-uitests-live -resultBundlePath /tmp/captainslog-current-device-uitests-live.xcresult -only-testing:CaptainsLogUITests` | Existing device-store audit verified local aggregate coverage. The latest May 18, 2026 current-head connected-phone UI-test attempt built and code signed the app plus UI-test runner with Apple Development, then stopped before launch at `Unlock Blake's iPhone 17 Pro Max (current) to Continue`; the waiting run was interrupted after the destination remained locked. The log is `/tmp/captainslog-current-device-uitests-live.log`. Final real-account tap-through remains open. | Human/device gate |

## Latest Readiness Result

The May 19, 2026 `Scripts/app_store_readiness_status.sh` run from clean local `main` currently:

- Passes command availability, Xcode iOS 26+ and macOS 26+ SDK checks, screenshot packet checks, screenshot text audit, preflight, required-reason audit, and credential-guard self-test.
- Confirms CaptainsLog is clean and synced with `origin/main`, and confirms Kit941 is clean and synced with `origin/main`. No linked-package upstream drift is currently reported.
- Reports platform availability directly: iPhone/iPad enabled through `TARGETED_DEVICE_FAMILY=1,2`, compatible Vision Pro support enabled through `SUPPORTS_XR_DESIGNED_FOR_IPHONE_IPAD=YES`, native Mac target present with automatic signing build settings but not Mac App Store ready, Developer Portal bundle/capability checks for native Mac, Watch, and TV bundle IDs when API credentials are present, Watch/TV target presence plus aggregate snapshot sync/read paths, companion icon/top-shelf assets, local App Store screenshot artifacts, explicit not-ready warnings, Return Watch/TV and Get Bananas Watch precedents when those projects are present, and the latest Vision, macOS, watchOS, and tvOS smoke evidence when present.
- Reports no App Store private keys inside the repo.
- Reports no OpenAI, Anthropic, or GitHub token-shaped source literals in app, test, doc, or script files.
- Uses staged screenshot package/review scripts so failed screenshot regeneration does not erase the last reviewed artifacts.
- Includes current post-fix build evidence for the DEBUG screenshot presentation path: macOS Debug and iOS simulator Debug both build successfully; the Mac screenshot script also exercises the DEBUG screenshot routes through app-window-only capture.
- Has current device-test evidence from `/tmp/captainslog-current-device-uitests-live.log` showing the latest connected-phone UI-test attempt built and signed but could not launch while the phone was locked.
- Has refreshed generic iOS Release archive evidence showing the app and linked `Kit941` package compile and package for device when code signing is disabled, plus current generic iOS Release builds after Kit941 commit `cbac28644070`, local Kit941 commit `cdc93e2`, and synced Kit941 commit `47a67ba` showing the linked package still compiles into CaptainsLog.
- Fails local readiness with 2 issues because the current IPA and export manifest are missing; readiness skips IPA local-check until a current IPA exists so the missing IPA is not counted twice. Kit941 source custody is currently clean and synced, so it is no longer an external gate in this session.
- A May 19 clean export attempt with existing 941-style App Store Connect API-key inputs archived successfully, but `xcodebuild -exportArchive` failed with `Cloud signing permission error`, `No signing certificate "iOS Distribution" found`, and an App Store provisioning profile missing the app's iCloud key-value store entitlement. A follow-up REST capability check confirms `ICLOUD` is enabled on the bundle ID, so the profile-specific blocker is stale/incomplete App Store profile regeneration rather than a missing bundle capability. After the stale local Store profile was moved aside, a clean export retry still archived successfully but failed export with `Cloud signing permission error` and `No profiles for 'com.blakecrosley.captainslog' were found`. The read-only remote signing checker now shows the visible iOS App Store profile is `INVALID` and no usable distribution certificate is visible to the selected API key. The missing IPA cannot be fixed locally until the App Store profile is regenerated/downloaded, plus either the API-key account has cloud-managed distribution certificate access or a matching local Apple Distribution/iOS Distribution identity with private key is installed.
- A precheck-bypassed native Mac export archives but fails export with `No Accounts`, no `Mac Installer Distribution` certificate, and no profiles for `com.blakecrosley.captainslog.mac`, confirming the Mac package also remains an external signing/profile gate.
- Reports 4 candidate App Store Connect `.p8` files outside the repo and requires `APP_STORE_CONNECT_API_KEY` plus `APP_STORE_CONNECT_API_ISSUER` selection before default export/upload auth. `APP_STORE_CONNECT_P8_FILE` can be omitted when the matching `AuthKey_<KEY_ID>.p8` exists in `~/.appstoreconnect/private_keys`. Existing 941 apps confirm the same Fastlane alias convention; Captain's Log now accepts those aliases, but reusing a key from another app must still use a private copy outside any git working tree because the credential guard rejects keys inside app repos or Fastlane folders.
- Reports external blockers for iOS distribution signing, Mac App Store application signing, Mac App Store installer signing, cloud-managed distribution certificate access or local distribution identity installation, App Store Connect app-record creation, manual App Store Connect fields including platform availability, upload/TestFlight processing, screenshot approval, legal/privacy review, and final real-account tap-through. Provider public ID is now only needed for the optional legacy `app-record-altool` path.

## Next Action

The next meaningful action is not more local feature work. Make one App Store export-signing path complete, then regenerate the current IPA. Either configure the selected `APP_STORE_CONNECT_API_KEY` and `APP_STORE_CONNECT_API_ISSUER` values with cloud-managed distribution certificate access, or install a matching local Apple Distribution/iOS Distribution identity with its private key, then rerun:

```sh
CAPTAINS_LOG_REQUIRE_CLEAN_EXPORT=1 Scripts/export_app_store_ipa.sh /tmp/captainslog-current-appstore-export
```

After that passes, rerun:

```sh
Scripts/app_store_signing_status.sh
Scripts/app_store_readiness_status.sh
Scripts/upload_app_store_ipa.sh local-check "/tmp/captainslog-current-appstore-export/Export/Captain's Log.ipa"
Scripts/check_app_store_connect_record.py
Scripts/upload_app_store_ipa.sh validate "/tmp/captainslog-current-appstore-export/Export/Captain's Log.ipa"
```
